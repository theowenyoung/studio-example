# GitHub Token for cloning private repos (if needed)
COMMON_OWEN_GH_TOKEN=

# Public URL
PUBLIC_URL=https://www${CTX_DNS_SUFFIX:-}.${CTX_ROOT_DOMAIN:-local.owenyoung.com}

# Meilisearch for docs-scraper (post-deploy index building)
MEILISEARCH_HOST_URL=https://meilisearch${CTX_DNS_SUFFIX:-}.${CTX_ROOT_DOMAIN:-owenyoung.com}
COMMON_MEILISEARCH_API_KEY=
MEILISEARCH_API_KEY=${COMMON_MEILISEARCH_API_KEY}
