# PostgreSQL admin password (for postgres superuser)
COMMON_POSTGRES_PASSWORD=
POSTGRES_PASSWORD=${COMMON_POSTGRES_PASSWORD}

# Shared application user configuration
COMMON_POSTGRES_APP_USER=app_user
COMMON_POSTGRES_APP_USER_PASSWORD=

# Demo database credentials (example of dedicated users)
DEMO_POSTGRES_USER_PASSWORD=
DEMO_POSTGRES_READONLY_PASSWORD=
# PostgreSQL connection (for psql client)
PGHOST=${CTX_PG_HOST:-localhost}
PGPORT=5432
PGUSER=postgres
PGPASSWORD=${COMMON_POSTGRES_PASSWORD}
PGDATABASE=postgres


# computed vars
# prod1 databases
HONO_DEMO_DB_NAME=hono_demo${CTX_DB_SUFFIX:-}

UMAMI_DB_NAME=umami${CTX_DB_SUFFIX:-}

N8N_DB_NAME=n8n${CTX_DB_SUFFIX:-}

OUTLINE_DB_NAME=outline${CTX_DB_SUFFIX:-}

JANT_DB_NAME=jant${CTX_DB_SUFFIX:-}

HONO_PROD2_DEMO_DB_NAME=hono_prod2_demo${CTX_DB_SUFFIX:-}


# prod2 databases
TEST_DB_NAME=test_db${CTX_DB_SUFFIX:-}
