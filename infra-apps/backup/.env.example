# AWS 凭证（从 Parameter Store 获取）
BACKUP_AWS_ACCESS_KEY_ID=
BACKUP_AWS_SECRET_ACCESS_KEY=

# 映射到 AWS CLI 标准变量
AWS_ACCESS_KEY_ID=${BACKUP_AWS_ACCESS_KEY_ID}
AWS_SECRET_ACCESS_KEY=${BACKUP_AWS_SECRET_ACCESS_KEY}

# S3 存储桶名称
BACKUP_S3_BUCKET=

# 自定义 S3 端点（可选，用于 MinIO 等 S3 兼容服务）
# 示例: https://s3.example.com
BACKUP_S3_ENDPOINT=

# ========================================
# 数据库连接（必填）
# ========================================

# PostgreSQL 管理员密码
COMMON_POSTGRES_PASSWORD=

# Redis 密码
COMMON_REDIS_PASSWORD=

# ========================================
# 计算变量 (Computed Logic)
# ========================================

# [Host Logic]
PG_HOST=${CTX_PG_HOST:-postgres}
REDIS_HOST=${CTX_REDIS_HOST:-redis}

# [Connection Strings]
# PostgreSQL 连接（备份所有数据库）
# 不需要指定数据库名，使用 pg_dumpall 备份所有数据库
POSTGRES_ADMIN_URL=postgresql://postgres:${COMMON_POSTGRES_PASSWORD}@${PG_HOST}:5432

# Redis 连接
REDIS_URL=redis://:${COMMON_REDIS_PASSWORD}@${REDIS_HOST}:6379


